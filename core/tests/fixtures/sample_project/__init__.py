"""A minimal MedalFlow project, used as the end-to-end fixture.

This mirrors what a consuming team's repo looks like: plain Python classes
carrying layer decorators, with methods returning SQL. Nothing here imports
test helpers, so discovery walks it exactly as it would a real project.
"""
